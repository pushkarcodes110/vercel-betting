# Web App (Vite + React)

Routes:

- `/` login
- `/home` main betting dashboard
- `/admin` internal admin dashboard

## Local

```bash
cd web
cp .env.example .env
npm install
npm run dev
```

Set `VITE_API_BASE=/api` when using same-origin API proxy.
